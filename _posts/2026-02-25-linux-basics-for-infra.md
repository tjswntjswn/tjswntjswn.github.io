---
layout: post
title: "개발자에서 인프라 엔지니어로: 서버 운영의 기초, 리눅스(Linux) 핵심 요약"
date: 2026-02-25 14:00:00 +0900
categories: [Infrastructure, Linux]
tags: [Linux, Infrastructure, Ubuntu, Server]
---

## 🚀 왜 인프라 엔지니어에게 리눅스가 필수인가?
인프라 엔지니어에게 리눅스는 단순한 운영체제가 아니라 **'서비스가 돌아가는 토대'**입니다. 윈도우와 달리 오픈 소스로서 자유도가 높고, 모든 설정을 코드로 관리(IaC)하거나 자동화 스크립트를 짜기에 최적화된 **CLI(Command Line Interface)** 환경을 제공하기 때문입니다.

### 리눅스의 심장: 커널(Kernel)
커널은 하드웨어(CPU, RAM, Disk)와 소프트웨어 사이의 중재자입니다. 인프라 엔지니어는 커널의 상태를 모니터링하고 설정을 최적화하여 서버의 안정성과 성능을 책임집니다.



---

## 📂 인프라 실무 핵심 명령어 가이드

개발자일 때보다 더 엄격하게 관리해야 하는 인프라 핵심 명령어들입니다.

### 1. 위치 확인 및 시스템 탐색
* **pwd**: 현재 작업 중인 디렉토리의 절대 경로를 출력합니다. 설정 파일을 수정하기 전, 내가 엉뚱한 곳에 있지 않은지 확인하는 필수 습관입니다.
* **ls -al**: 파일 목록뿐만 아니라 **권한(rwx)**과 **소유자**를 상세히 확인합니다. 서비스 실행 권한 문제를 해결하는 첫걸음입니다.
* **cd [경로]**: /etc(설정), /var/log(로그) 등 주요 시스템 경로로 이동합니다.

### 2. 파일 및 디렉토리 관리 (안정성 우선)
* **mkdir**: 새로운 디렉토리를 생성합니다.
* **cp -r [원본] [대상]**: 파일이나 디렉토리를 복사합니다. **실무에서는 설정 파일을 수정하기 전 반드시 .bak 파일을 만드는 습관이 중요합니다.**
* **mv [원본] [대상]**: 위치 이동 또는 이름 변경 시 사용합니다.
* **rm -rf [대상]**: **주의!** 하위 디렉토리를 포함해 강제로 삭제합니다. 인프라 환경에서는 한 번의 실수가 치명적이므로 항상 경로를 재확인해야 합니다.

### 3. Vim (텍스트 에디터) 활용
인프라 설정의 90%는 서버 터미널 내 Vim에서 이루어집니다.
* **i**: 입력 모드 (텍스트 편집 시작)
* **Esc**: 명령 모드 (편집 종료 및 명령 준비)
* **:wq**: 저장 후 종료 (Write & Quit)
* **:q!**: 저장하지 않고 강제 종료

---

## 📦 패키지 설치와 관리자 권한

### 패키지 매니저 (APT)
Ubuntu 계열 서버에서 소프트웨어를 설치하고 업데이트하는 표준 도구입니다.

<코드 박스>
sudo apt update          # 패키지 목록 업데이트
sudo apt install -y curl # 네트워크 테스트 도구 설치
</코드 박스>

### 관리자 권한 (Sudo)
"SuperUser Do"의 약자로, 시스템의 핵심 설정을 변경할 때 일시적으로 권한을 획득합니다. 인프라 관리자는 최소 권한 원칙을 지키며 꼭 필요한 순간에만 sudo를 사용해야 합니다.

---

## 🚦 실시간 모니터링 및 장애 대응
단순 실행을 넘어 서버의 '건강 상태'를 체크하는 역량이 필요합니다.

* **top / htop**: 실시간 시스템 리소스(CPU, 메모리) 사용량을 확인하여 과부하 원인을 찾습니다.
* **tail -f [로그파일]**: 실시간으로 쌓이는 서비스 로그를 감시합니다. 장애 발생 시 가장 먼저 확인해야 할 도구입니다.
* **Ctrl + C**: 실행 중인 프로세스를 강제 종료하고 터미널로 복귀합니다.



---

## 💡 마치며: 시각의 전환
개발자에게 리눅스가 '코드를 실행하는 도구'였다면, 인프라 엔지니어에게는 **'안정적으로 유지해야 할 그릇'**입니다. 명령어 하나하나가 시스템 전체에 미치는 영향을 고민하는 습관이 중요합니다. 

다음 포스팅에서는 리눅스 보안의 핵심인 **권한(Permission) 체계**와 **Nginx 웹 서버 설정**에 대해 다뤄보겠습니다.